A pod definition file `nginx.yaml` is given. Create a pod using the file.


Only create the POD for now. We will inspect its status next.

```bash
kubectl apply -f nginx.yaml
```

What is the status of the created POD?

```bash
kubectl get po
```

Why is the POD in a pending state? Inspect the environment for various kubernetes control plane components.

No schedulable node available
```bash
Node: <none>
Events: <none>
```
```bash
kubectl describe node controlplane | grep Taints
```

Manually schedule the pod on node01. Delete and recreate the POD if necessary.

```bash
controlplane ~ ➜  cat nginx.yaml 
---
apiVersion: v1
kind: Pod
metadata:
  name: nginx
spec:
  nodeName: node01 # ADD PARTICULAR NODE NAME TO SCHEDULE POD
  containers:
  -  image: nginx
     name: nginx
```
```bash
controlplane ~ ➜  cat nginx.yaml 
---
apiVersion: v1
kind: Pod
metadata:
  name: nginx
spec:
  nodeName: contolplane # IF WE WANT TO SCHEDULE AT CONTROLPLANE NODE
  containers:
  -  image: nginx
     name: nginx
```