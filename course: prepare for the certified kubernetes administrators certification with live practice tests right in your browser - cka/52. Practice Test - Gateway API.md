which api resource define in kubernetes for Gateway
- Gateway
- In Kubernetes, the Gateway API introduces new API resources under the group `gateway.networking.k8s.io`
- Main API resources(kinds) defined for Gateway
    - `GatewayClass`
        - Defines a class of Gateways, similar to how StorageClass works for volumes.
        - Example: GatewayClass provided by Istio, NGINX, or another controller.
    - `Gateway`
        - An instance of a GatewayClass.
        - Configures listeners (hostnames, ports, protocols) and binds routes.
        - ```yaml
            apiVersion: gateway.networking.k8s.io/v1
            kind: Gateway
            metadata:
            name: my-gateway
            spec:
            gatewayClassName: nginx
            listeners:
                - name: http
                protocol: HTTP
                port: 80
                hostname: example.com
            ```
    - `HTTPRoute`
        - Defines HTTP routing rules (match host, path, headers → forward to Services).
    - `TCPRoute`
        - Defines routing rules for raw TCP connections.
    - `UDPRoute`
        - Defines routing rules for raw UDP traffic.
    - `TLSRoute`
        - For TLS-level routing.
    - `ReferenceGrant`
        - Allows cross-namespace references (security boundary for routes/services).


What is the purpose of the `allowedRoutes` field in a Gateway?



What protocols are NOT supported by the Kubernetes Gateway API?

- Protocols Supported
    - HTTP (HTTPRoute)
    - HTTPS (via TLS on HTTPRoute)
    - TLS (terminated or passthrough using TLSRoute)
    - TCP (TCPRoute)
    - UDP (UDPRoute)